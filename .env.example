# amagi 本地开发的环境变量模板
#
# **这个文件进 git，`.env` 不进**（`.gitignore` 里有一条光秃秃的 `.env`）。
# 用法：复制成 `.env` 再填值，或者直接在控制台界面里填 —— 那边会写回 `.env`。
#
#   cp .env.example .env
#
# 控制台：`pnpm console:server` + `pnpm console`（两个进程，都要起）

# ─────────────────────────────────────────────────────────────────
# 各平台的 cookie。控制台用它们发请求录样本
# ─────────────────────────────────────────────────────────────────
#
# 从浏览器开发者工具里复制整条 `Cookie` 请求头。**整条**，不要只挑几个字段 ——
# 平台的风控会看那些看起来无关的字段（`ttwid` / `odin_tt` / `msToken` 这类）。
#
# 没有某个平台的 cookie 也能用控制台：那些端点大多会拿回登录页或风控页，
# 而入库判定会把它们拒掉、界面上说清是哪一类。
#
# 值用双引号包起来 —— cookie 里有 `;` 和空格，不包会被很多解析器截断。

AMAGI_COOKIE_BILIBILI=""
AMAGI_COOKIE_DOUYIN=""
AMAGI_COOKIE_KUAISHOU=""
AMAGI_COOKIE_XIAOHONGSHU=""

# ─────────────────────────────────────────────────────────────────
# 其它
# ─────────────────────────────────────────────────────────────────

# core 的调试脚本（`pnpm dev`）的日志级别：error / warn / info / debug
# LOG_LEVEL="debug"
