// 改名原因：叶子文件名原先缺 `_V0`（`NewLoginQrcode.ts`），目录本身已符合约定，
// 这次只把它补成 `NewLoginQrcode_V0.ts`。对外名字 `BiliNewLoginQrcode` 与
// `BilibiliReturnTypeMap.loginQrcode` 都没动。
import { BiliNewLoginQrcode_V0 } from './NewLoginQrcode_V0'

export type BiliNewLoginQrcode = BiliNewLoginQrcode_V0
